server:
  port: 8999

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///drug?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=UTC
    username: # 你的数据库账号
    password: # 你的密码
    type: com.alibaba.druid.pool.DruidDataSource
  mvc:
    static-path-pattern: /static/**
  thymeleaf:
    prefix: classpath:/templates
    cache: false